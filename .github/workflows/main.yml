name: simple workflow

on:
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
    - name: Run Hello world
      run: echo "Hello World"
      shell: bash

  welcome:
    runs-on: ubuntu-latest

    steps:
    - name: run welcome world
      run: echo "Welcome World"
      shell: bash
  
